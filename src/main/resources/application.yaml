spring:
  application:
    name: ecom-config-server

  cloud:
    config:
      server:
        git:
          uri: https://github.com/Fazil-1123/Ecom-config-repo
          username: ${GIT_USER}
          password: ${GIT_TOKEN}
server:
  port: 8181
